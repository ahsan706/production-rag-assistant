# Screenshots

This directory contains lightweight portfolio screenshot assets checked into the repository.

For fresh browser screenshots, run the stack and capture:

- `http://localhost:3000`
- `http://localhost:3000/chat`
- `http://localhost:3000/retrieval`

The SVG files are static renderings of the current validated UI states.
